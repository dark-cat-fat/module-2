Функция находит и возвращает сумму разности соседних элементов массива.

Время работы = O(n), тк цикл не является вложенным и ему нужно просто пройти по порядку своих элемтов.

Память = O(1), тк мы храним одну переменную и поэтому память будет не изменной.

